- hosts: localhost
  become: true
  # become_user: cg
  vars_files:
    - config.yml
  pre_tasks:
    - name: update repositories
      apt: update_cache=yes
      changed_when: False

  roles:
    - dev
    - gui
    - i3
    - network
    - system
    - toolbox
