---
- name: Install wallpaper manager
  apt:
    name: 'hsetroot'

- name: Copy wallpaper
  copy:
    src: wallpaper.jpg
    dest: "/home/{{ os_user }}/.wallpaper.jpg"
    force: yes
    owner: "{{ os_user }}"
    group: "{{ os_user }}"
    mode: '0600'
